#include <stdio.h>

viod _strtok(const char *str, char delimeter)
{
int len = strlen(str);
int i, j;

for (i = 0; i <= len; i++)
{
j = 0;
if (str[i] == delimeter || str[i] == '\0')
word[j] = '\0';
if (j > 0)
{
printf("%s\n", word);
j = o;
}
else
{
word[j++] = str[i]
}
}
